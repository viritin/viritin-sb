<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.7-SNAPSHOT</version>
        <relativePath /> <!-- lookup parent from repository -->
    </parent>

    <groupId>org.example</groupId>
    <artifactId>flattened-flow-only</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>jar</packaging>

    <description>Example of Vaadin Flow (only) project with "cloud native setup".
        Use this as such (if you don't want to depend on provided parent project)
        or use it as a stub for your own parent project for Vaadin Flow apps.</description>

    <properties>
        <java.version>17</java.version>
        <vaadin.version>24.9.0</vaadin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.vaadin</groupId>
                <artifactId>vaadin-bom</artifactId>
                <version>${vaadin.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>
    
    <dependencies>
        <!-- Custom version of vaadin starter. This brings in relevant dependencies
        for Spring Boot and Vaadin Flow (without Hilla and excludes for vaadin-dev). -->
        <dependency>
            <groupId>in.virit</groupId>
            <artifactId>spring-boot-starter-flow</artifactId>
            <version>1.0.2-SNAPSHOT</version>
        </dependency>
        <!-- Now also other devtools but spring-boot-devtools can work as one would expect -->
        <!-- Define the vaadin-dev as runtime & optional, can't accidentally code against its
             classes and will NOT end up on production.
         -->
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>vaadin-dev</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>
        <!-- Devtools is cool, especially if you don't have JRebel -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>
        <!-- You might want to test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        
    </dependencies>
    
    <build>
        <plugins>
            <plugin>
                <groupId>com.vaadin</groupId>
                <artifactId>vaadin-maven-plugin</artifactId>
                <version>${vaadin.version}</version>
                <executions>
                    <execution>
                        <id>frontend</id>
                        <!-- Not yet on compile, but before actual packaging -->
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>prepare-frontend</goal>
                            <goal>build-frontend</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <!-- The new flag in SB 3.5.7+ to make other all optional dependencies behave as they should -->
                    <!-- TODO check if this is still needed in future versions 🤷‍♂️ -->
                    <includeOptional>false</includeOptional>
                </configuration>
            </plugin>
        </plugins>
    </build>
    
</project>